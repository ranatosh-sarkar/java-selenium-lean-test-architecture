<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="60">
  <Properties>
    <!-- Project-root logs folder -->
    <Property name="LOG_DIR">${sys:user.dir}/logs</Property>
    <!-- Timestamp captured at JVM start; used in the base filename -->
    <Property name="CURRENT_TS">${date:yyyyMMdd_HHmmss}</Property>
    <Property name="CONSOLE_PATTERN">[%d{HH:mm:ss}] [%-5level] %c{1} - %msg%n</Property>
    <Property name="FILE_PATTERN">[%d{yyyy-MM-dd HH:mm:ss}] %-5p %c{1}:%L - %m%n</Property>
  </Properties>

  <Appenders>
    <Console name="Console" target="SYSTEM_OUT">
      <PatternLayout pattern="${CONSOLE_PATTERN}"/>
    </Console>

    <!-- Active file: logs/test_<current date+time>.log -->
    <RollingFile name="FileLogger"
                 fileName="${LOG_DIR}/test_${CURRENT_TS}.log"
                 filePattern="${LOG_DIR}/test_${CURRENT_TS}-%d{yyyy-MM-dd-HH-mm}-%i.log.gz">
      <PatternLayout pattern="${FILE_PATTERN}"/>
      <Policies>
        <!-- Roll every minute -->
        <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
        <!-- Or at ~5MB -->
        <SizeBasedTriggeringPolicy size="5MB"/>
      </Policies>
      <DefaultRolloverStrategy max="10"/>
    </RollingFile>
  </Appenders>

  <Loggers>
    <Logger name="com.automationframework" level="info" additivity="false">
      <AppenderRef ref="Console"/>
      <AppenderRef ref="FileLogger"/>
    </Logger>

    <Root level="warn">
      <AppenderRef ref="Console"/>
      <AppenderRef ref="FileLogger"/>
    </Root>
  </Loggers>
</Configuration>
